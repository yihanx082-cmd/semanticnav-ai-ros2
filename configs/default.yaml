model:
  name: yolo26n.pt
  confidence: 0.25
  image_size: 640

tracker:
  name: bytetrack.yaml

depth:
  enabled: true
  model_name: depth-anything/Depth-Anything-V2-Small-hf
  frame_interval: 3
  near_threshold: 0.70
  far_threshold: 0.35

mapping:
  rows: 30
  columns: 30
  obstacle_inflation_cells: 2

output:
  root: outputs
