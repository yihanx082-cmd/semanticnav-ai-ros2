# SemanticNav AI 项目计划

> 基于视觉感知、自然语言理解与路径规划的室内机器人语义导航软件原型

## 1. 项目定位

### 1.1 项目目标

构建一个不依赖实体机器人和专用深度相机的纯软件 Demo，完整展示以下数据流：

```text
自然语言任务
    ↓
结构化任务解析
    ↓
室内视频目标检测与跟踪
    ↓
单目深度估计或公开 RGB-D 数据定位
    ↓
局部语义栅格地图
    ↓
A* / Nav2 路径规划
    ↓
仿真执行与动作建议
```

项目第一版定位为“机器人语义感知与导航软件原型”，不宣称已经接入真实底盘，也不把单目深度描述为精确的米制测距。

### 1.2 最终演示

用户输入：

```text
去沙发附近，但绕开宠物和容易缠绕的物体。
```

系统完成：

1. 将指令转换为结构化任务；
2. 从视频中检测并跟踪人物、宠物、椅子、沙发等目标；
3. 估计目标远近，或从公开 RGB-D 数据计算三维坐标；
4. 生成带有语义代价的局部俯视地图；
5. 规划避开指定目标的路径；
6. 在 Streamlit 和 Gazebo/RViz2 中展示结果；
7. 导出结果视频、JSON、地图、路径及性能报告。

## 2. 范围与边界

### 2.1 第一版必须实现

- 本地 MP4 上传与逐帧读取；
- YOLO 目标检测；
- ByteTrack 多目标跟踪；
- 边界框、类别、置信度、轨迹 ID 和轨迹线可视化；
- 结果视频与逐帧 JSON 导出；
- FPS、平均延迟和 P95 延迟统计；
- 地面或可通行区域分割；
- 单目相对深度估计；
- 公开 RGB-D 数据的三维反投影；
- 二维语义栅格地图；
- A* 路径规划；
- 自然语言任务到受约束 JSON 的解析；
- ROS 2 + Gazebo + Nav2 仿真闭环；
- 单元测试、基准测试、README 和 Demo 视频。

### 2.2 第一版明确不做

- 不购买 RGB-D 相机、Jetson、激光雷达或移动底盘；
- 不训练大型视觉语言模型；
- 不让 LLM 直接生成电机控制命令；
- 不把动作建议伪装成真实底盘控制；
- 不承诺电线、拖鞋等细小类别可由通用预训练模型稳定识别；
- 不以 Isaac Sim 作为必需依赖；
- 不在第一版加入机械臂、MoveIt 2 或多机器人协同；
- 不进行没有校准数据支持的 INT8 量化。

## 3. 现有设备与算力方案

### 3.1 本机配置

| 项目 | 当前配置 | 用途 |
|---|---|---|
| CPU | Intel Core Ultra 5 125H | 开发、视频解码、CPU 推理、ROS 2、轻量仿真 |
| 内存 | 32 GB | 足够第一版开发和 Gazebo 轻量场景 |
| GPU | Intel Arc 核显 | 桌面显示与有限加速，不用于 CUDA/TensorRT |
| 硬盘 | D 盘约 292 GB 可用 | 代码、数据集、模型和实验输出 |
| 系统 | Windows | 阶段 1～5 开发；ROS 2 阶段使用 Ubuntu |

### 3.2 算力分配

```text
现有 Windows 笔记本
├─ Python 与 Streamlit 开发
├─ OpenCV 视频处理
├─ YOLO 轻量模型 CPU 推理
├─ ByteTrack、地图与 A*
└─ 数据集整理和测试

Ubuntu 24.04 原生环境
├─ ROS 2 Jazzy
├─ Gazebo
├─ RViz2
└─ Nav2

按需云 GPU（可选）
├─ 自定义 YOLO 微调
├─ 分割模型微调
└─ 批量性能实验
```

第一版不需要购买 NVIDIA 电脑。只有开始自定义模型训练时，才按小时租用 8～16 GB 显存的云 GPU；模型较大或输入分辨率较高时再选择 24 GB 显存实例。

### 3.3 存储规划

```text
D:\semanticnav-ai-ros2\
├─ datasets\          # 不提交 Git
├─ models\            # 不提交 Git
├─ outputs\           # 不提交 Git
├─ assets\samples\    # 只保留小型演示素材
└─ 项目源代码
```

长期目标：C 盘保留至少 80 GB 空闲，D 盘保留至少 100 GB 空闲。若安装 Ubuntu 双系统，建议为 Ubuntu 单独预留 150～200 GB。

## 4. 技术栈

| 模块 | 技术选择 |
|---|---|
| 语言 | Python 3.11；ROS 2 性能敏感节点可后续使用 C++ |
| 包管理 | `venv` + `pip`，锁定依赖版本 |
| 视频处理 | OpenCV |
| 检测 | Ultralytics YOLO 轻量模型 |
| 跟踪 | ByteTrack |
| 分割 | YOLO Seg 或轻量 SegFormer |
| 单目深度 | Depth Anything 系列轻量模型或同类方案 |
| RGB-D 验证 | 公开 RGB-D 数据集与合成深度数据 |
| 任务解析 | JSON Schema + 云端 LLM；离线规则解析作为回退 |
| 地图 | NumPy 二维语义栅格 |
| 规划 | A*；后期接入 Nav2 |
| 前端 | Streamlit |
| 机器人中间件 | ROS 2 Jazzy |
| 仿真 | Gazebo + RViz2 |
| 测试 | pytest、rosbag2、ROS 2 launch testing |
| 导出 | JSON、MP4、PNG、CSV |

## 5. 目标目录结构

```text
semanticnav-ai-ros2/
├─ app/
│  └─ streamlit_app.py
├─ semanticnav/
│  ├─ config.py
│  ├─ types.py
│  ├─ video/
│  │  ├─ reader.py
│  │  └─ writer.py
│  ├─ perception/
│  │  ├─ detector.py
│  │  ├─ tracker.py
│  │  ├─ segmenter.py
│  │  └─ monocular_depth.py
│  ├─ rgbd/
│  │  ├─ camera.py
│  │  ├─ depth_filter.py
│  │  └─ projection.py
│  ├─ mapping/
│  │  ├─ grid.py
│  │  └─ semantic_cost.py
│  ├─ planning/
│  │  └─ astar.py
│  ├─ language/
│  │  ├─ schema.py
│  │  ├─ parser.py
│  │  └─ safety.py
│  ├─ pipeline/
│  │  └─ video_pipeline.py
│  └─ serialization/
│     └─ results.py
├─ ros2_ws/src/
│  ├─ semanticnav_msgs/
│  ├─ semanticnav_perception/
│  ├─ semanticnav_mapping/
│  ├─ semanticnav_language/
│  └─ semanticnav_bringup/
├─ simulation/
│  ├─ worlds/
│  ├─ models/
│  └─ config/
├─ configs/
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ fixtures/
├─ benchmarks/
├─ assets/
│  └─ samples/
├─ datasets/          # gitignored
├─ models/            # gitignored
├─ outputs/           # gitignored
├─ docs/
├─ .gitignore
├─ pyproject.toml
├─ requirements.txt
├─ README.md
└─ PROJECT_PLAN.md
```

## 6. 数据接口

### 6.1 检测与跟踪结果

```json
{
  "frame_index": 42,
  "timestamp_s": 1.4,
  "inference_ms": 68.2,
  "objects": [
    {
      "track_id": 7,
      "class_id": 56,
      "class_name": "chair",
      "confidence": 0.91,
      "bbox_xyxy": [315.0, 180.0, 510.0, 620.0]
    }
  ]
}
```

### 6.2 RGB-D定位结果

```json
{
  "track_id": 7,
  "class_name": "chair",
  "frame_id": "camera_link",
  "timestamp_s": 1.4,
  "position_m": {"x": 0.21, "y": 0.34, "z": 1.82},
  "distance_m": 1.86,
  "valid_depth_ratio": 0.78,
  "localization_valid": true
}
```

### 6.3 语言任务结果

```json
{
  "target": "sofa",
  "target_relation": "near",
  "avoid_classes": ["cat", "dog", "cable", "shoe"],
  "preferred_distance_m": 0.8,
  "speed_mode": "slow",
  "clarification_required": false
}
```

LLM 输出必须先通过 Schema 与安全规则验证，验证失败时停止规划并要求澄清。LLM 不得输出轮速、转向角或 `/cmd_vel`。

## 7. 阶段计划

## 阶段 0：仓库与开发环境

**周期：** 2～3 天  
**目标：** 建立所有后续模块共享的工程基线。

### 工作项

- [ ] 创建目标目录结构；
- [ ] 配置 Python 3.11 虚拟环境；
- [ ] 添加 `.gitignore`，排除数据、权重、输出、密钥和虚拟环境；
- [ ] 建立 `pyproject.toml`、`requirements.txt` 和 pytest；
- [ ] 定义检测、轨迹、三维目标和语义任务的数据类型；
- [ ] 准备一个短视频、小型合成深度图和相机内参作为测试夹具；
- [ ] 添加基础日志和配置加载；
- [ ] 编写环境安装与测试命令。

### 验收标准

- [ ] 全新环境按 README 可完成安装；
- [ ] `pytest -q` 可执行并通过；
- [ ] 数据集、模型权重、输出视频和 `.env` 不会被 Git 跟踪；
- [ ] 配置文件中不存在绝对用户路径。

### 里程碑提交

```text
chore: initialize project structure and test setup
```

---

## 阶段 1：视频检测与跟踪 MVP

**周期：** 1～2 周  
**目标：** 输入任意可正常解码的 MP4，输出带持续轨迹 ID 的视频和 JSON。

### 工作项

- [ ] 实现视频元数据读取和逐帧迭代；
- [ ] 集成 YOLO 轻量预训练模型；
- [ ] 集成 ByteTrack；
- [ ] 绘制边界框、类别、置信度、轨迹 ID 和历史轨迹线；
- [ ] 记录视频读取、预处理、推理、跟踪和绘制耗时；
- [ ] 计算平均 FPS、平均延迟和 P95 延迟；
- [ ] 实现结果 JSON 序列化；
- [ ] 写出与原视频帧率和尺寸一致的结果视频；
- [ ] 在 Streamlit 中实现上传、运行、预览和下载；
- [ ] 测试空视频、损坏视频、零检测帧和多目标帧。

### 验收标准

- [ ] 支持常见 H.264 MP4；
- [ ] 输出视频可播放且总帧数与输入合理一致；
- [ ] JSON 中每帧含时间戳、性能数据和目标列表；
- [ ] 同一连续可见目标的轨迹 ID 基本稳定；
- [ ] 零检测帧不会导致程序崩溃；
- [ ] 页面显示平均 FPS 与推理延迟；
- [ ] 视频读取、序列化和边界框坐标测试通过。

### 里程碑提交

```text
feat: add video detection and tracking pipeline
```

---

## 阶段 2：分割与单目深度

**周期：** 2 周  
**目标：** 从普通室内视频估计可通行区域和相对远近关系。

### 工作项

- [ ] 集成轻量地面/场景分割模型；
- [ ] 输出类别掩码、置信度图和叠加图；
- [ ] 集成轻量单目深度估计；
- [ ] 对每个跟踪目标计算中心区域稳健深度统计；
- [ ] 将深度归一化为“近、中、远”三级风险；
- [ ] 把分割和深度结果加入 Streamlit 四画面视图；
- [ ] 测量检测、分割和深度串行运行时的总延迟；
- [ ] 增加跳帧、降低输入分辨率和按模块开关策略。

### 验收标准

- [ ] 输出分割掩码和深度可视化；
- [ ] 深度结果仅描述相对距离，不标注虚假的米制精度；
- [ ] 目标中心区域存在异常值时仍能给出稳健估计；
- [ ] 无有效深度时标记为未知，而不是填入零距离；
- [ ] 页面能够分别启用和关闭检测、分割与深度。

### 里程碑提交

```text
feat: add segmentation and monocular depth estimation
```

---

## 阶段 3：公开 RGB-D 数据与三维定位

**周期：** 2 周  
**目标：** 在无硬件条件下验证真实相机模型、深度过滤与三维反投影。

### 工作项

- [ ] 确定一个许可清晰的公开 RGB-D 数据集；
- [ ] 实现 RGB、深度、时间戳和相机内参读取；
- [ ] 支持深度单位到米的统一转换；
- [ ] 对检测框中心区域过滤 0、NaN、无穷和超范围值；
- [ ] 使用有效深度的中位数计算目标深度；
- [ ] 实现像素到相机坐标的反投影；
- [ ] 输出 `X/Y/Z`、欧氏距离、有效深度比例和有效标记；
- [ ] 使用合成三维点和相机内参验证反投影；
- [ ] 记录公开数据上的定位稳定性和失败案例。

### 反投影公式

```text
X = (u - cx) × Z / fx
Y = (v - cy) × Z / fy
Z = depth(u, v)
distance = sqrt(X² + Y² + Z²)
```

### 验收标准

- [ ] 合成三维点投影后再反投影，误差仅来自浮点计算；
- [ ] 深度越界、缺失或有效比例不足时返回无效结果；
- [ ] 输出坐标明确使用 `camera_link` 坐标系；
- [ ] RGB 与深度尺寸不一致时显式报错或使用明确的对齐步骤；
- [ ] 报告平均绝对误差、相对误差或重复测量标准差；
- [ ] README 明确数据来源、许可和实验限制。

### 里程碑提交

```text
feat: add rgbd localization with robust depth filtering
```

---

## 阶段 4：语义栅格地图与 A* 规划

**周期：** 2～3 周  
**目标：** 将目标、障碍物和可通行区域转换为可解释的二维语义代价地图。

### 工作项

- [ ] 定义地图尺寸、分辨率、原点和坐标约定；
- [ ] 实现米制坐标与栅格索引互换；
- [ ] 将未知、可通行、普通障碍和高风险语义映射为不同代价；
- [ ] 对障碍物按机器人半径和安全余量膨胀；
- [ ] 实现四邻域或八邻域 A*；
- [ ] 阻止路径穿过不可通行单元；
- [ ] 起点或终点被占据时返回可解释错误；
- [ ] 在 Streamlit 中显示地图、障碍、起终点和路径；
- [ ] 记录路径长度、规划时间、最小障碍距离和失败原因。

### 验收标准

- [ ] 合成地图上的路径不穿越障碍；
- [ ] 完全封闭地图正确返回无路径；
- [ ] 坐标与栅格转换可逆误差不超过半个栅格；
- [ ] 障碍膨胀后路径保持规定安全距离；
- [ ] 相同输入得到确定性的规划结果；
- [ ] 地图图例能够解释每种语义代价。

### 里程碑提交

```text
feat: build semantic grid and astar planner
```

---

## 阶段 5：自然语言任务解析

**周期：** 1～2 周  
**目标：** 将中英文导航指令转换为受约束、可验证的任务对象。

### 工作项

- [ ] 定义 JSON Schema；
- [ ] 实现规则解析器作为离线基线；
- [ ] 接入云端 LLM 结构化输出；
- [ ] 检查缺少目标、多个冲突目标和未知关系；
- [ ] 检查危险、越权或直接控制电机的指令；
- [ ] 将“容易缠绕的东西”等概念映射为可配置类别集合；
- [ ] LLM 超时、网络失败或输出非法时回退到澄清状态；
- [ ] 编写不少于 20 条中英文测试指令；
- [ ] 在页面中并排展示原始指令、解析 JSON 和安全检查结果。

### 验收标准

- [ ] 所有成功解析结果均通过 Schema；
- [ ] 缺少目标时 `clarification_required=true`；
- [ ] 冲突约束不进入规划模块；
- [ ] LLM 不输出轮速、转向角或电机命令；
- [ ] API 密钥只从环境变量或安全配置读取；
- [ ] 至少 20 条测试覆盖中文、英文、歧义、冲突和危险指令。

### 里程碑提交

```text
feat: add safe structured language task parser
```

---

## 阶段 6：端到端 Streamlit Demo

**周期：** 1～2 周  
**目标：** 将前五个阶段组合成一个可重复演示的软件产品。

### 页面布局

```text
左侧栏
├─ 视频与数据源选择
├─ 模型与阈值设置
├─ 自然语言任务
└─ 输出目录与运行按钮

主区域
├─ 原始/检测跟踪画面
├─ 分割与深度结果
├─ 俯视语义地图与路径
├─ 结构化任务 JSON
└─ FPS、延迟、目标数量和规划指标
```

### 工作项

- [ ] 实现统一配置与运行状态；
- [ ] 支持普通视频、公开 RGB-D 样例和预计算结果三种模式；
- [ ] 支持中止处理并保留已完成结果；
- [ ] 将各模块错误转换为页面可理解的提示；
- [ ] 导出视频、JSON、地图 PNG、路径 CSV 和性能 CSV；
- [ ] 添加一键运行的小型示例；
- [ ] 编写端到端集成测试；
- [ ] 录制 30～60 秒 Demo。

### 验收标准

- [ ] 新用户按 README 在 15 分钟内启动示例；
- [ ] 示例无需专用硬件；
- [ ] 同一次运行的全部输出使用唯一运行 ID；
- [ ] 输出文件可下载且字段一致；
- [ ] 页面不会把单目深度显示为精确米制真值；
- [ ] Demo 明确标注“仿真路径/动作建议，未接真实底盘”。

### 里程碑提交

```text
feat: integrate semantic navigation streamlit demo
```

---

## 阶段 7：ROS 2、Gazebo 与 Nav2 仿真

**周期：** 4～5 周  
**目标：** 在 Ubuntu 24.04 + ROS 2 Jazzy 中完成语义导航仿真闭环。

### ROS 2 节点

```text
camera_node
detector_node
tracker_node
segmenter_node
depth_localizer_node
language_task_node
semantic_costmap_node
navigation_node
```

### 主要 Topic

```text
/camera/color/image_raw
/camera/depth/image_raw
/camera/camera_info
/perception/detections
/perception/tracks
/perception/semantic_mask
/perception/objects_3d
/task/semantic_goal
/costmap/semantic_obstacles
/cmd_vel
```

### TF 结构

```text
map
└─ odom
   └─ base_link
      ├─ camera_link
      └─ laser_link
```

### 工作项

- [ ] 安装 Ubuntu 24.04、ROS 2 Jazzy、Gazebo、RViz2 和 Nav2；
- [ ] 定义自有消息或采用标准消息；
- [ ] 将纯 Python 核心逻辑封装为 ROS 2 节点；
- [ ] 确保所有消息包含时间戳和 `frame_id`；
- [ ] 构建带 RGB-D 相机的差速机器人仿真模型；
- [ ] 发布对齐的 RGB、Depth、CameraInfo 和点云；
- [ ] 将语义障碍物写入 Nav2 代价地图；
- [ ] 将结构化任务转换为目标位置和导航约束；
- [ ] 配置全局规划器、局部控制器和恢复行为；
- [ ] 记录 rosbag2、TF、Topic 频率和端到端延迟；
- [ ] 建立静态障碍、动态障碍、目标不存在和路径封闭测试场景。

### 验收标准

- [ ] 单个 launch 文件可以启动全部仿真组件；
- [ ] TF 树连通且时间戳一致；
- [ ] 语义障碍在 Nav2 代价地图中可见；
- [ ] 机器人能够导航至目标附近而非目标几何中心；
- [ ] 动态障碍出现时能够减速、停止或重规划；
- [ ] 目标不存在或路径不可达时安全停止；
- [ ] 至少运行 20 次标准场景并统计成功率和碰撞率。

### 里程碑提交

```text
feat: add ros2 gazebo semantic navigation simulation
```

---

## 阶段 8：性能评测、文档与作品集

**周期：** 1～2 周  
**目标：** 形成可复现、可解释、可用于面试展示的完整项目材料。

### 工作项

- [ ] 固定测试视频、RGB-D 样例和仿真场景；
- [ ] 记录检测、跟踪、深度、地图和规划的分模块耗时；
- [ ] 记录平均延迟、P50、P95、FPS、内存占用和输出大小；
- [ ] 记录定位误差、路径长度、最小障碍距离、成功率和碰撞率；
- [ ] 对关闭跟踪、关闭语义代价和不同障碍膨胀半径进行消融实验；
- [ ] 整理失败案例与原因；
- [ ] 完善 README、架构图、节点图、启动命令和限制；
- [ ] 制作 Demo GIF 和 30～60 秒演示视频；
- [ ] 检查许可证、数据来源和模型来源；
- [ ] 完成一次从干净环境开始的复现测试。

### 验收标准

- [ ] README 含一条最短启动命令；
- [ ] 所有表格填写真实测量值，不保留虚构指标；
- [ ] Demo、代码版本、配置和结果能够对应；
- [ ] 失败案例至少覆盖漏检、遮挡、深度异常和路径不可达；
- [ ] GitHub 仓库不包含密钥、大型数据、模型权重或输出视频；
- [ ] 已知限制明确说明未接入实体底盘和真实 RGB-D 相机。

### 里程碑提交

```text
docs: publish reproducible demo and benchmark report
```

## 8. 时间表

按每周 15～20 小时估算：

| 周次 | 阶段 | 主要交付物 |
|---|---|---|
| 第 1 周 | 阶段 0 | 工程基线、测试环境、数据接口 |
| 第 2～3 周 | 阶段 1 | YOLO + ByteTrack 视频 MVP |
| 第 4～5 周 | 阶段 2 | 分割与单目相对深度 |
| 第 6～7 周 | 阶段 3 | RGB-D反投影与误差报告 |
| 第 8～10 周 | 阶段 4 | 语义栅格与 A* 路径 |
| 第 11～12 周 | 阶段 5 | 安全的语言任务解析 |
| 第 13～14 周 | 阶段 6 | Streamlit 端到端 Demo |
| 第 15～19 周 | 阶段 7 | ROS 2 + Gazebo + Nav2 |
| 第 20～21 周 | 阶段 8 | Benchmark、README、演示视频 |

### 精简交付节点

- **3 周：** 可展示的检测与跟踪网页；
- **7 周：** 可展示深度和三维定位的软件版本；
- **14 周：** 完整 Streamlit 语义导航原型；
- **21 周：** ROS 2 仿真闭环和完整作品集。

## 9. 测试计划

### 9.1 单元测试

| 模块 | 必测内容 |
|---|---|
| 视频 | 元数据、逐帧读取、损坏文件、空视频 |
| 序列化 | 空目标、多目标、浮点精度、字段一致性 |
| 坐标 | `xyxy`合法性、裁剪、中心点、归一化 |
| 深度过滤 | 0、NaN、无穷、越界、中位数、有效比例 |
| 反投影 | 主点、图像边缘、已知三维点、单位换算 |
| 地图 | 米制坐标与栅格互换、边界、障碍膨胀 |
| A* | 直线路径、绕障、封闭地图、非法起终点 |
| 语言 | Schema、缺失目标、冲突、危险指令、双语 |

### 9.2 集成测试

- 视频 → 检测 → 跟踪 → JSON；
- RGB-D → 检测区域 → 稳健深度 → 三维坐标；
- 三维对象 → 语义地图 → A*路径；
- 指令 → 结构化任务 → 语义代价配置；
- Streamlit 示例 → 完整导出文件；
- Gazebo传感器 → ROS 2节点 → Nav2代价地图 → 导航结果。

### 9.3 回归测试数据

仓库只保留体积较小且许可允许的测试夹具。完整数据集由下载脚本获取，并校验文件哈希，避免把大文件直接提交到 GitHub。

## 10. 评价指标

### 10.1 感知

| 指标 | 说明 |
|---|---|
| 检测数量与置信度 | 检查目标输出合理性 |
| ID Switch | 衡量跟踪身份切换 |
| 平均/P95延迟 | 避免只报告最好结果 |
| FPS | 端到端处理速度 |
| 分割 mIoU | 有标注数据时测量 |

### 10.2 三维定位

| 指标 | 说明 |
|---|---|
| 绝对误差 | 预测距离与真值差值 |
| 相对误差 | 绝对误差除以真值 |
| 有效深度比例 | 参与估计的有效像素占比 |
| 重复测量标准差 | 静态目标定位稳定性 |

### 10.3 规划与导航

| 指标 | 说明 |
|---|---|
| 路径长度 | 规划路径总长度 |
| 规划耗时 | A*或Nav2规划时间 |
| 最小障碍距离 | 路径安全裕量 |
| 成功率 | 到达目标附近的实验比例 |
| 碰撞率 | 发生碰撞的实验比例 |
| 重规划次数 | 动态场景响应情况 |

## 11. 风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| CPU推理过慢 | 页面卡顿 | 使用轻量模型、降低分辨率、跳帧和缓存 |
| 通用模型没有电线/拖鞋类别 | Demo漏检 | 先声明类别边界，后期少量定制数据微调 |
| 单目深度尺度不确定 | 无法精确测距 | 只报告相对远近；米制实验使用公开RGB-D数据 |
| RGB与Depth不对齐 | 三维坐标错误 | 使用已对齐数据或明确实现配准并可视化验证 |
| LLM输出不稳定 | 错误任务进入规划 | Schema验证、安全规则和澄清状态三层防护 |
| Windows下ROS设备兼容问题 | 联调困难 | ROS 2阶段使用原生Ubuntu 24.04 |
| 硬盘空间不足 | 数据或环境损坏 | 数据放D盘，定期清理缓存，保持空间余量 |
| 项目范围膨胀 | 无法按期交付 | 每阶段独立验收，未通过前不进入下一阶段 |

## 12. 采购计划

### 第一版

**必须购买：无。**

可选费用：

| 项目 | 是否必要 | 预算 |
|---|---|---:|
| 云GPU | 仅自定义模型训练时 | 按小时使用，设定费用上限 |
| LLM API | 可用规则解析器替代 | 少量测试费用 |
| 1～2 TB移动SSD | 仅空间不足时 | 按实际需要购买 |

### 完成纯软件版本以后

只有在以下条件全部满足后，才考虑购买 RGB-D 相机：

- 视频检测与跟踪已经稳定；
- 深度过滤和反投影单元测试全部通过；
- 公开 RGB-D 数据演示已经完成；
- 确实需要增加真实传感器验证来提升作品说服力。

Jetson、激光雷达和移动底盘均不属于本计划的必需采购。

## 13. Git与版本管理

### `.gitignore`至少包含

```gitignore
.venv/
venv/
__pycache__/
*.pyc
.env
.streamlit/secrets.toml
datasets/
models/
outputs/
runs/
*.pt
*.onnx
*.engine
*.mp4
*.avi
*.bag
*.db3
```

### 分支与提交

- 每个阶段使用独立功能分支；
- 每个可独立验证的任务单独提交；
- 提交前运行相关测试；
- 合并前运行完整测试与一个小型端到端示例；
- 不提交API密钥、个人路径、大型数据和模型权重。

## 14. 阶段门禁

每个阶段只有满足以下条件才能视为完成：

- [ ] 功能与本阶段范围一致；
- [ ] 相关单元测试和集成测试通过；
- [ ] README 或模块文档已更新；
- [ ] 真实测量结果已记录；
- [ ] 已知限制和失败案例已记录；
- [ ] Git工作树无意外生成的大文件；
- [ ] 里程碑提交已创建。

## 15. 最终完成定义

项目最终必须满足：

- [ ] 用户可以输入中英文自然语言导航任务；
- [ ] 任务被转换为受Schema约束的语义目标与避障规则；
- [ ] 视频中的常见室内目标可以被检测并跟踪；
- [ ] 普通视频可提供相对深度，公开RGB-D数据可提供三维坐标；
- [ ] 系统可以生成带语义代价的二维地图和无碰撞路径；
- [ ] Streamlit可以展示完整数据流并导出结果；
- [ ] Gazebo中的机器人可以通过Nav2完成语义目标导航；
- [ ] 项目报告FPS、延迟、定位误差、路径指标和导航成功率；
- [ ] 所有结果可由README中的命令复现；
- [ ] 文档明确说明软件仿真边界，不伪装为实体机器人控制。

## 16. 推荐执行顺序

```text
阶段0 工程基线
    ↓
阶段1 YOLO + ByteTrack MVP
    ↓
阶段2 分割 + 单目相对深度
    ↓
阶段3 公开 RGB-D 三维定位
    ↓
阶段4 语义地图 + A*
    ↓
阶段5 语言任务解析
    ↓
阶段6 Streamlit 端到端演示
    ↓
阶段7 ROS 2 + Gazebo + Nav2
    ↓
阶段8 Benchmark + GitHub作品集
```

第一交付目标不是“完整机器人”，而是用三周完成一个稳定、可下载结果、可测性能的 YOLO + ByteTrack 视频 MVP。后续所有模块都以这个可运行基线逐步扩展。
